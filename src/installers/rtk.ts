export async function installRtk() {
  return {
    success: false,
    manual: true,
    error:
      "Automatic RTK installation is not implemented yet. Install RTK manually and ensure rtk.exe is on PATH."
  };
}